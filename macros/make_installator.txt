file (READ zarlok_installer.iss.in ISSFILE)
file (READ build_version_number.txt DIST_VERSION)

string (REPLACE "\@CMAKE_CURRENT_BINARY_DIR\@" "${CMAKE_CURRENT_BINARY_DIR}" ISSFILE ${ISSFILE})
string (REPLACE "\@CMAKE_CURRENT_SOURCE_DIR\@" "${CMAKE_CURRENT_SOURCE_DIR}" ISSFILE ${ISSFILE})
string (REPLACE "\@MINGW_BINARY_DIR\@" "${MINGW_BINARY_DIR}" ISSFILE ${ISSFILE})
string (REPLACE "\@DIST_VERSION\@" "${DIST_VERSION}" ISSFILE ${ISSFILE})
string (REPLACE "\@QT_LIBRARY_DIR\@" "${QT_LIBRARY_DIR}" ISSFILE ${ISSFILE})

write_file (zarlok_installer.iss ${ISSFILE})